<Window x:Class="B2CPolicyEditor.Views.TOUSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:B2CPolicyEditor.Views"
        Title="Terms of Use Version" Height="300" Width="400"
        mc:Ignorable="d" 
        d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Text="Current version number:" />
            <TextBox Margin="10,0,0,0"  Grid.Column="1" Text="{Binding CurrVersionId, Mode=OneWay}" IsEnabled="false"/>
            <TextBlock Grid.Row="1" Text="New version:" />
            <TextBox Margin="10,0,0,0" Grid.Column="1" Grid.Row="1" Text="{Binding NewVersionId}" />
        </Grid>
        <Button Content="OK" Command="{Binding OnOK}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="30" Padding="10"/>
    </Grid>
</Window>
